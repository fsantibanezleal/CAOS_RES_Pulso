// English locale (source of truth per ADR-0011). ES mirrors these keys.
export const en = {
  brand: 'Pulso',
  tagline: 'The geological DNA of fractured reservoirs',
  nav: {
    app: 'App',
    introduction: 'Introduction',
    methodology: 'Methodology',
    implementation: 'Implementation',
    experiments: 'Experiments',
    benchmark: 'Benchmark',
  },
  header: { theme: 'Toggle theme', lang: 'Language', github: 'Source on GitHub', architecture: 'How it was built' },
  common: {
    validated: 'VALIDATED',
    notValidated: 'not validated',
    silhouette: 'silhouette',
    coverage: 'coverage',
    target: 'target',
    source: 'source',
    oodRate: 'out-of-catalogue rate',
    topControl: 'top control',
    real: 'Real 4TU',
    synthetic: 'Synthetic',
    simulated: 'open-DARTS',
    loading: 'loading…',
    error: 'error',
    panelError: 'This panel could not render for the selected case.',
  },
  app: {
    title: 'Workbench',
    intro:
      'Pick how to work: explore a committed case from the corpora, or open the live lab and tune a synthetic curve. Classical + SOTA + novel tools run in a TypeScript engine; the learned deep models run via onnxruntime-web on the ONNX trained offline.',
    modeLabel: 'Mode',
    workbench: 'Workbench families',
    case: 'Case',
    laneLive: 'live in-browser',
    lanePrecompute: 'precomputed artifact',
    ro: { category: 'source', lane: 'lane' },
    mode: {
      explore: 'Explore a case',
      live: 'Live lab',
      exploreHelp: 'Pick a case from the corpora (real 4TU, field pumping tests, simulated DFM physics, analytic families, benchmarks) and work through its ensemble, assignment, methods and physics.',
      liveHelp: 'Tune the fracture-flow parameters and run every tool in the ladder live on the curve (classical + SOTA + learned ONNX + novel).',
    },
    fam: {
      ensemble: 'Ensemble',
      assignment: 'Assignment',
      methods: 'Methods',
      physics: 'Physics',
      context: 'Context',
    },
    tool: {
      catalogue: 'Catalogue',
      explorer: 'Ensemble explorer',
      dtwmap: 'DTW heatmap',
      shape: 'Shape space',
      classify: 'Classify',
      attribution: 'Attribution',
      attrplus: 'Attribution+',
      agreement: 'Method agreement',
      sim: 'DFM simulation',
      darts: 'DARTS validation',
      network: 'Fracture network',
    },
    cat: {
      analytic: 'Analytic families',
      real: 'Real 4TU corpus',
      field: 'Field pumping tests',
      dfm: 'Simulated DFM physics',
      dfn: 'Fracture networks',
      darts: 'open-DARTS validation',
      bench: 'Full-corpus benchmarks',
    },
    explorer: {
      desc: 'The whole committed ensemble: every member curve coloured by GeoType, and the per-cluster p10/p50/p90 envelope bands that summarise the full population. Hover to read each cluster median at that dimensionless time.',
      both: 'Members + envelopes',
      envelopes: 'Envelopes only',
      members: 'Members only',
      population: 'population',
      committed: 'committed',
      aria: 'Ensemble explorer: member curves and per-cluster p10/p50/p90 envelopes',
      drawnCap: 'Showing {k} of {n} committed member curves (the envelopes summarise every member).',
      membersNote: 'members',
    },
    attrplus: {
      predTitle: 'Predictability vs K',
      predDesc: 'For each candidate K, the RF held-out BALANCED accuracy of predicting the GeoType labels from the physical descriptors (balanced so the minority GeoTypes count, not just the majority baseline), alongside the silhouette. Where accuracy peaks is where the labels are most attributable; K* is the chosen catalogue size.',
      silhouette: 'silhouette',
      rfAcc: 'RF balanced accuracy',
      romTitle: 'ROM descriptor sensitivity',
      romDesc: 'A reduced-order surrogate (RF on descriptors -> GeoType) swept one descriptor at a time across its p5..p95 range (others at the median): the sensitivity is the largest swing in any GeoType probability across the sweep (a partial-dependence measure, informative even when the argmax never flips). The top bars are the physical knobs that move a curve across GeoType boundaries.',
      romNone: 'No varying descriptors to sweep for this case.',
      dualTitle: 'Dual-representation conformal (beyond SOTA)',
      dualDesc: 'Our novel layer conformalizes jointly across shape space (DTW distance to medoid) AND physics-descriptor space (RF implausibility). A curve is accepted for a GeoType only if it is BOTH shape-consistent AND descriptor-consistent, so a shape-match with implausible physics is flagged, beyond single-score conformal and beyond RF+SHAP (which has no coverage guarantee).',
      metric: 'metric',
      shapeOnly: 'shape-only',
      dual: 'dual (shape ∩ physics)',
      coverage: 'empirical coverage',
      meanSet: 'mean prediction-set size',
      caught: 'caught as out-of-catalogue by physics',
      examples: 'right-shape-wrong-physics examples',
      unavailable: 'Attribution+ unavailable',
    },
    dtwmap: {
      desc: 'The pairwise DTW distance matrix, ordered by GeoType. Bright (yellow) = similar shape, dark (purple) = distant; the clusters appear as bright blocks on the diagonal, and the white lines are the cluster boundaries. Hover a cell to read the pair distance and the two GeoTypes.',
      size: 'matrix',
      dmax: 'max distance',
      similar: 'similar',
      distant: 'distant',
      pair: 'pair',
      distance: 'DTW distance',
      aria: 'DTW distance heatmap, cluster-ordered, viridis colormap',
      note: 'Perceptually-uniform viridis (never rainbow). Large ensembles ship a capped random subsample of the matrix (noted above).',
    },
    catalogue: {
      title: 'GeoType catalogue',
      desc: 'The recurring flow behaviours discovered by DTW k-medoids over this ensemble. Bold curves are the medoids (the representative response of each GeoType); faint curves are members. Hover a curve to read its value.',
      k: 'GeoTypes (K)',
      members: 'members',
    },
    classify: {
      title: 'Classify a curve (conformal)',
      desc: 'Assign a curve to the catalogue with a coverage guarantee. The prediction set contains every GeoType consistent with the curve at the chosen confidence; an empty set is an honest out-of-catalogue flag.',
      pick: 'Curve to classify',
      pred: 'Nearest GeoType',
      set: 'Prediction set',
      ood: 'Out of catalogue',
      pvalues: 'p-values',
    },
    attribution: {
      title: 'What controls each GeoType',
      desc: 'A Random Forest + SHAP attribution of the GeoType labels to the fracture-network / parameter descriptors, gated on held-out accuracy: if the forest cannot predict the labels, the importances are withheld rather than reported as noise.',
      gate: 'RF accuracy gate',
      passed: 'passed',
      withheld: 'withheld (gate not passed)',
      descriptor: 'descriptor',
      importance: 'importance',
    },
    methods: {
      desc: 'The SOTA clustering alternatives run offline on this exact ensemble, scored against the reference DTW k-medoids catalogue. Silhouette measures cluster quality in DTW geometry; the Adjusted Rand Index (ARI) is chance-corrected agreement with the reference labels (1.0 = identical grouping, ~0 = random). Where methods agree, the behaviours are robust; where only one does, the catalogue should be read with more caution.',
      reference: 'Reference',
      method: 'Method',
      silhouette: 'Silhouette',
      ari: 'ARI vs reference',
      note: 'Note',
      subsampled: 'subsampled',
      subsampledHelp: 'For large corpora the comparison runs on a seeded 600-curve subsample (the reference labels are subsampled identically, so the ARI stays fair).',
      legend: 'ARI: green >= 0.75 (strong agreement), amber >= 0.5 (partial), red < 0.5 (disagrees). HDBSCAN discovers its own K (no fixed count); an empty or noisy result is an honest signal, not a failure.',
    },
    representations: {
      desc: 'Four complementary ways to lay out and describe the same committed ensemble, all computed offline and aligned to the member curves. Switch the 2D layout to see how each method groups the responses; points are coloured by GeoType and the medoids are ringed.',
      unavailable: 'This layout is not available for this case (the optional library was absent at bake time).',
      fpcaTitle: 'Functional PCA modes',
      fpcaDesc: 'The dominant modes of variation of the derivative response (eigen-shapes on the log-time grid) and the fraction of ensemble variance each explains. Mode 1 is the strongest axis along which the curves differ.',
      catch22Title: 'catch22 feature signature',
      catch22Desc: 'The catch22 canonical time-series features that most separate the GeoTypes (largest between-cluster spread of the mean, normalised by within-cluster spread). Each column is a cluster mean.',
      catch22Unavailable: 'catch22 features are unavailable for this case (pycatch22 was absent at bake time).',
      feature: 'feature',
    },
    network: {
      title: 'Discrete fracture network',
      desc: 'A GeoDFN realization from this ensemble (geologically consistent: stress-shadow placement, Von-Mises orientations, power-law lengths). Transient simulation on these networks is the open-DARTS phase.',
      fractures: 'fractures',
      pending: 'transient simulation: pending (open-DARTS Step B)',
    },
    anchor: {
      title: 'open-DARTS validation',
      desc: 'A real single-phase drawdown simulated in open-DARTS, in dimensionless form, next to the analytical infinite-acting solution. The Bourdet derivative plateaus at 0.5 (the radial-flow signature); the skin-corrected pressure matches the line source.',
    },
  },
  intro: {
    title: 'Do reservoirs have a geological DNA?',
    lead:
      'A fractured reservoir answers a pressure test with a curve whose SHAPE encodes the geometry of its fracture network. FlowDNA turns those pressure-transient responses into a catalogue of GeoTypes (recurring flow behaviours discovered by unsupervised learning) and identifies which fracture properties control each behaviour, to constrain geological uncertainty early and guide data acquisition.',
    s1h: 'The question',
    s1: 'Two reservoirs with different fracture networks can produce visibly different pressure responses; two with similar networks produce similar ones. If the shape of the response reflects the underlying physical system, can machine learning read the geology back out of it, without labels?',
    s2h: 'The approach',
    s2: 'Generate geologically consistent discrete fracture networks (GeoDFN), simulate their pressure transients, cluster the response SHAPES with dynamic time warping and k-medoids into a catalogue of GeoTypes, then attribute each GeoType to the fracture-network properties that drive it with Random Forest + SHAP. Assign new responses to the catalogue with a conformal coverage guarantee.',
    s3h: 'Why it generalizes',
    s3: 'Nothing in the method is specific to reservoirs. Any response signal whose shape reflects the underlying physical system (hydrogeology pumping tests, thermal response tests, geothermal well tests) can be catalogued the same way.',
    sourceh: 'Source',
    source:
      'FlowDNA reproduces and extends Kamel Targhi, Rongier, Bruna, Daniilidis & Geiger, "Unsupervised learning for geologically consistent fluid flow analysis in fractured reservoirs", Computational Geosciences 30:57 (2026), open access.',
  },
  method: {
    title: 'Methodology',
    lead: 'Five stages, each an engine-agnostic, seeded, testable step. The shape machinery is the pygeotypes library; the domain engines are GeoDFN (generation) and open-DARTS (simulation).',
    s1h: '1 · The shape carriers: pressure transients and their derivatives',
    s1: 'The information about the reservoir is in the shape of the log-log diagnostic pair (Δp, p′) where p′ = dΔp/d ln t is the Bourdet derivative. Flow regimes map to derivative signatures: a radial plateau at 0.5, a dual-porosity valley, a linear 1/2 slope. Clustering the derivative removes level and isolates the regime transitions.',
    s2h: '2 · Dynamic Time Warping',
    s2: 'Two responses with the same regime sequence but shifted transition times are the same behaviour at a different scale. DTW aligns the shapes before measuring distance; a Sakoe-Chiba band bounds the warping so early-time cannot match late-time.',
    dtw: 'd_{\\text{DTW}}(x,y)=\\min_{\\pi\\in\\mathcal{A}}\\sqrt{\\sum_{(i,j)\\in\\pi}(x_i-y_j)^2}',
    s3h: '3 · PAM k-medoids → the GeoType catalogue',
    s3: 'k-medoids needs only the distance matrix and returns actual observed curves as prototypes: physically meaningful representatives you can plot. K is chosen by the silhouette computed directly from the DTW matrix.',
    s4h: '4 · Conformal assignment (the novel layer)',
    s4: 'Nearest-medoid always answers, even for a curve unlike anything in the catalogue. Split-conformal prediction returns a prediction set that covers the true GeoType with probability at least 1−α, and an empty set is an honest out-of-catalogue flag.',
    conf: 'p_g(x)=\\frac{|\\{s\\in S_g: s\\ge d(x,m_g)\\}|+1}{|S_g|+1}',
    s5h: '5 · Attribution: Random Forest + SHAP',
    s5: 'A Random Forest predicts the GeoType labels from the fracture descriptors; TreeSHAP ranks which descriptors drive each GeoType, cross-checked against permutation importance and gated on held-out accuracy so noise is never reported as a finding.',
    s6h: '6 · The learned tier (deep models → ONNX → live)',
    s6: 'Four SOTA deep models are trained on the GPU (torch, .venv-train cu124) on the GeoType curves and exported to ONNX (parity < 1e-4) so they run live in the browser via onnxruntime-web: an InceptionTime classifier (Ismail Fawaz 2020; multi-scale Inception modules + residuals), a PatchTST-lite transformer classifier (Nie 2023; patchified series + attention), a deep convolutional autoencoder (a latent embedding + a reconstruction-error anomaly / out-of-distribution score), and a TS2Vec-style contrastive encoder (Yue 2022; dilated convolutions, NT-Xent, for nearest-neighbour retrieval). The offline pipeline does the hard processing; the browser runs the exported models. Held-out metrics are reported honestly per model.',
    refh: 'References',
  },
  impl: {
    title: 'Implementation',
    lead: 'A frozen offline pipeline that bakes committed artifacts, and a static web app that replays them. The pipeline is the product; the app is a read-only projection.',
    lanesh: 'Three lanes',
    lanes:
      'Offline (heavy engines: GeoDFN, open-DARTS, the DTW matrix, PAM, RF/SHAP) bakes the artifacts. Live (pure numpy/scipy + pygeotypes, Pyodide-safe) can regenerate an analytic curve and conformally classify it in the browser. Replay (this app) reads only the committed artifacts.',
    stagesh: 'Staged pipeline',
    stages: 'preprocess → feature extraction → train → infer → evaluate → export. Each stage is deterministic and seeded; the same (spec, seed) produces a byte-identical artifact.',
    contractsh: 'Two data contracts',
    contracts:
      'Ingestion (raw → pipeline): curves and specs are accepted or rejected with a reason, never silently coerced. Artifact (pipeline → web): a compact trace + manifest whose schema a TypeScript type mirrors, so any drift fails the build.',
    stackh: 'Stack',
    stack: 'Python 3.12 offline (pygeotypes, GeoDFN, open-DARTS, dtaidistance, scikit-learn, shap); React 19 + Vite + react-router + react-i18next + KaTeX for the web.',
  },
  exp: {
    title: 'Experiments',
    lead: 'Per-case results across all GeoType study lanes: analytic Warren-Root studies, the real 4TU fractured-reservoir corpus, real field pumping tests (AquiferTypes), and the simulated open-DARTS DFM studies (plus the open-DARTS anchor and the GeoDFN network ensembles). Each number is read from the committed manifest.',
    studies: 'GeoType studies (analytic / real 4TU / field / simulated DFM)',
    findingsh: 'Honest findings',
    f1: 'DTW sensitivity is timing > family > depth: valley timing and the homogeneous-vs-dual-porosity family are cleanly attributed; pure valley depth (an amplitude feature) is the hard case where the RF gate correctly withholds attribution.',
    f2: 'Real transients cluster far more cleanly than the analytic ensembles (silhouette 0.58 to 0.86 vs 0.13 to 0.25). The controlling descriptor is configuration-dependent; on the high-permeability config, fracture intensity is the top control, partially reproducing the paper.',
    f3: 'The open-DARTS single-phase drawdown reproduces infinite-acting radial flow: the Bourdet derivative plateaus at 0.5 and the skin-corrected pressure matches the analytical line source to ~1%.',
    f4: 'The simulated open-DARTS DFM studies cluster the fracture-network transients into GeoTypes and pass the MRST fidelity gate (ensemble-median Bourdet-derivative shape correlation ~0.92 vs the paper reference); attribution surfaces the well-to-fracture distance and the fracture aperture as top controls.',
    f5: 'The methodology transfers to REAL aquifer field data (welltestpy pumping tests): both well-characterized sites are predominantly one infinite-acting-radial AquiferType with a few outlier wells, and the pooled cross-site study is an honest null result (the type is not predictable from radial distance, pumping rate or site).',
  },
  bench: {
    title: 'Benchmark',
    lead: 'The heavier offline questions that span cases, not a single App view: cross-configuration GeoType stability, K-selection sweeps, and the engine-validation status.',
    crossh: 'Cross-configuration stability',
    cross:
      'The paper reports ~64% GeoType membership retained across the three matrix-fracture permeability configurations. Reproducing that over the full corpus (all ~4768 curves per dataset, at the paper\'s K) is the offline Benchmark run, distinct from the per-config App cases.',
    kh: 'K selection',
    k: 'Silhouette over the candidate K range per case, with the elbow of the total cost as the cross-check.',
    engineh: 'Engine validation',
    engine: 'The open-DARTS drawdown is validated against the analytical solution (Bourdet derivative plateau 0.5, ~1% skin-corrected match) before any DFN complexity. The DFN-simulated transients (Step B) are now MRST-fidelity-gated: the DFM ensemble-median Bourdet derivative falls inside the paper\'s MRST reference band (p5-p95 coverage) with a scale-aligned shape correlation ~0.92 for Datasets A and C. The methodology also transfers to REAL aquifer field data (welltestpy pumping tests), where both well-characterized sites are predominantly one infinite-acting-radial AquiferType and the cross-site study is an honest null.',
  },
  live: {
    intro: 'This is the live lab. Drag the fracture-flow parameters and every tool in the ladder recomputes in the browser on the tuned curve: classical diagnostics and the DTW/conformal tools in a TypeScript engine, the learned SOTA models (InceptionTime, PatchTST, autoencoder, TS2Vec) via onnxruntime-web. No server, no replay.',
    controls: 'Live controls (drag to recompute)',
    homogeneous: 'homogeneous (no fractures, the null response)',
    loadingModels: 'loading the ONNX learned models…',
    tierClassical: 'Classical', tierSota: 'SOTA', tierNovel: 'novel', tierLearned: 'Learned (ONNX)',
    tierShape: 'Shape matching', ladder: 'Method ladder',
    m: {
      diagnostics: 'Bourdet diagnostics', dtw: 'DTW → GeoType', conformal: 'Conformal',
      inceptiontime: 'InceptionTime', patchtst: 'PatchTST', autoencoder: 'Autoencoder', contrastive: 'Contrastive',
    },
    diagDesc: 'The classical log-log diagnostic workbench: dimensionless pressure, its Bourdet derivative (p′) and the second derivative (p″, curvature). Flow regimes are auto-detected and shaded on the plot; the live Warren-Root and Theis fits recover the parameters straight from the curve.',
    diag: {
      bestFit: 'best analytic fit',
      curvature: '(curvature, read off the smoothed derivative)',
      detected: 'detected regimes',
      none: 'no clear single regime',
      recovery: 'Live parameter recovery',
      recoveryDesc: 'The analytic responses are fitted to the tuned curve live (coarse grid + local refine). A fitter that recovers the values you set is the honest test; the lower-RMSE model is the one the data supports.',
      model: 'model', recovered: 'recovered', truth: 'you set',
    },
    dtwDesc: 'SOTA: dynamic-time-warping distance from the tuned curve to each baked GeoType medoid. The nearest medoid is the assignment.',
    conformalDesc: 'The novel layer: split-conformal p-values against the baked class-conditional calibration. The prediction set covers the true GeoType at 1−α; an empty set is an honest out-of-catalogue flag.',
    incepDesc: 'Learned (SOTA): an InceptionTime classifier (Ismail Fawaz 2020) trained on the GPU (torch → ONNX) runs live via onnxruntime-web. Multi-scale Inception modules + residuals give class probabilities on the tuned curve.',
    patchtstDesc: 'Learned (SOTA): a PatchTST-lite transformer (Nie 2023) trained on the GPU (torch → ONNX) runs live. It patchifies the curve and attends across patches, a transformer counterpart to InceptionTime.',
    aeDesc: 'Learned: a deep convolutional autoencoder (ONNX, live) embeds the curve and scores its reconstruction error, an anomaly / out-of-distribution signal. The latent point moves as you tune.',
    contrastiveDesc: 'Learned (SOTA): a TS2Vec-style encoder (Yue 2022, dilated-conv, trained contrastively with NT-Xent) embeds the curve; nearest-neighbour retrieval against the baked training cloud returns the closest GeoType.',
    nearest: 'nearest medoid', set: 'prediction set', ood: 'out of catalogue',
    predicted: 'predicted GeoType', acc: 'held-out acc', probability: 'probability',
    anomaly: 'reconstruction anomaly', latentDim: 'latent dim', retrieved: 'retrieved GeoType',
    latentNote: 'The autoencoder latent space (first two dims); your curve is the white point.',
    trainingCurves: 'training curves', loading: 'computing…',
  },
  arch: {
    title: 'How FlowDNA was built',
    close: 'Close',
    t1: 'The app',
    t2: 'The three lanes',
    t3: 'The science',
    t4: 'Data contracts',
    p1: 'A static React SPA replays committed, audited artifacts. It never computes the heavy pipeline; the optional live lane runs only pure-Python shape code in the browser.',
    p2: 'Offline bakes the artifacts with the heavy SOTA engines. Live regenerates an analytic curve + conformally classifies it (Pyodide). Replay reads the committed trace + manifest.',
    p3: 'GeoDFN generates geologically consistent networks; open-DARTS simulates their transients; pygeotypes preprocesses, clusters, assigns (conformal) and attributes (RF/SHAP).',
    p4: 'An ingestion contract gates any input data; an artifact contract (trace + manifest, TypeScript-mirrored) gates what the web reads. A drift on either fails the build.',
  },
};

export type Dict = typeof en;
